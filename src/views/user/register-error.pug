doctype html
html
  head
    title Registration Error - OpenID Provider
    meta(name="viewport", content="width=device-width, initial-scale=1")
    style.
      body {
        font-family: Arial, sans-serif;
        max-width: 400px;
        margin: 50px auto;
        padding: 20px;
        text-align: center;
      }
      .error {
        color: #e74c3c;
        margin: 20px 0;
      }
      a {
        color: #3498db;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }

  body
    h1 Registration Failed
    p.error Email already registered
    a(href=`/user/register?redirect_uri=${redirectUri}&client_id=${clientId}&state=${state}&scope=${scope}`) Try again
